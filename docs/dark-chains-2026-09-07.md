# 暗黑事件链状态 · 2026-09-07

本文件记录 `wt/dark` 分支的实现范围、给工程师 A 的接线方式、以及验收命令的当前结果。施工依据为《结局与暗黑链实施合同 · 2026-09-07》第四节 DK-1～DK-14、第五节附件规则、第六节接线清单「工程师 B」栏。设计表为 `13_事件库/09_暗黑链事件.md`。

## 一、新增内容

| 项目 | 数量 | 位置 |
|---|---:|---|
| 暗黑链事件 E-213–E-258 | 46 | `src/content/events/authored.json` 的 `events` 数组 |
| 夜班专属条目 E-259–E-274 | 16 | 同上 |
| 新增选项 | 174 | 链 126 项，夜班 48 项 |
| 新增事实 | 114 | 合同 4.15 的 124 项中，由事件选项写入的部分 |
| 事件库总数 | 274 | 原 212 |

合同 4.15 的 124 项事实里，另外 10 项不由事件选项写入：`伤医-施暴者已确定` 由医闹三拍的 escalated 出口与导演的入口判定写入；`伤医-受伤生还`、`伤医-手部功能损失`、`伤医-长期行动障碍`、`伤医-抢救无效`、`伤医-家属证言`、`精神-无法复岗`、`精神-长期症状`、`身体-救回`、`身体-抢救无效`、`身体-上限永久下调` 由链末结算按此前已写入的事实取值；`伴侣-在册` 由开局「轮转登记」页的伴侣设置写入（`engine.ts:204`，工程师 A 已实现；选「无」时不写，DK-10 与 END-18 不出现）。

## 二、接线

`src/content/events/dark-chains.ts` 导出两个函数，签名与合同第六节一致，`engine.ts` 里的调用点由工程师 A 完成（`63f46d2`），本分支合并后已经接通：

```ts
export function darkChainEntry(r: Run, kind: 'san' | 'stamina'): Card | undefined
export function darkChainResolve(r: Run): void
```

- `darkChainEntry(r,kind)` 在 `interrupt()` 里、该项再次归零且准备调用 `stop(r,kind)` 之前调用。返回 `undefined` 时按原有的 `stop(r,kind)` 收口，首次归零仍走 E-197～E-202。返回一张卡时，引擎把它插到当前位置作为急性事件卡，写入 `dark-chain:<kind>`，本局继续；该事实存在期间同一项归零不再重复触发。
- `darkChainResolve(r)` 在每次 `interrupt()` 与 `stop()` 里调用。它把结果标记写进 `r.facts`，写入格式与 `engine.ts` 的 `flag()` 一致（`{day, source:'dark-chain', sequence}`），已存在的记录保持原值与原日期。链末步骤走完时另写 `dark-chain-resolved:<kind>`，引擎读到它就 `stop(r,kind)`，结局由 `earlyEnding` 按合同第二节的优先级选取。
- 链末步骤的判据：SAN 侧为 `event-seen:E-232`、`event-seen:E-237`、`自杀-死亡确认`、`天台-中止当班`、`精神-无法复岗`、`精神-长期症状` 任一成立；体力侧为 `event-seen:E-239`、`身体-救回`、`身体-抢救无效` 任一成立。三条支线（DK-5 收在 E-232，DK-6 收在停止当班，DK-7 收在死亡确认）都算 SAN 侧的终点。
- 只需要标记本身而不写入时用 `darkChainOutcome(r): Effects`。
- 三条链的结果都由此前的求援、退出、就医与陪同事实决定，没有额外掷骰。
- 入口卡自带 `EventContext`（关系、抑郁、三维、现金、已写入的事实），所以 E-230 的「给家里打个电话」只对家人仍有联系的一局出现。导演的 `forceZeroEvent` 按同一规则派发这两张卡，两条路径给出的事件 ID 一致。

入口事件的选择规则：

| 调用 | 条件 | 返回 |
|---|---|---|
| `darkChainEntry(r,'san')` | `recordedSanBreaks(r) < 2` | `undefined` |
| | `r.depression >= 70` 且未写入 `危机-支持联系已建立`、`精神-持续住院` | E-233（DK-6 天台危机） |
| | 其余再次归零 | E-230（DK-5 严重精神崩溃） |
| `darkChainEntry(r,'stamina')` | `r.exhausted < 1` | `undefined` |
| | `r.exhausted >= 1` | E-238（DK-8 抢救失败） |

`engine.interrupt` 在取卡之前累加 `sanBreaks`，因此第二次 SAN 归零读到的值为 2；体力计数在取卡之后累加，第二次体力归零读到的值仍为 1。两处判据按此写。

链末结算的取值规则：

| 链 | 判据 | 结果 |
|---|---|---|
| DK-1 伤医 | 已求援且（陪同离院或避开单独会面） | `伤医-受伤生还` |
| | 只成立一项，且求援已发出／安保在冲突当日或次日到场／有他人介入之一成立 | `伤医-受伤生还` ＋ `伤医-手部功能损失` |
| | 只成立一项，且三者都不成立 | `伤医-受伤生还` ＋ `伤医-长期行动障碍` |
| | 两项都不成立 | `伤医-抢救无效` |
| | 以上三种去向 | 另写 `伤医-家属证言` |
| DK-8 抢救失败 | 已写入 `身体-已求援` 或 `身体-已就医` | `身体-救回` |
| | 未求援未就医，且本局出现过 `健康-确诊`／`纸带`／`未就诊` | `身体-抢救无效` |
| | 未求援未就医，身体线从未开启 | `身体-救回` ＋ `身体-上限永久下调` |
| DK-5 精神崩溃 | 已写入 `精神-持续住院` | `精神-无法复岗` |
| | 已写入 `危机-评估已做` | `精神-长期症状` |
| | 走到 `危机-失联` | 不写结果，由 DK-6 与 DK-7 承接 |

合同第八节第 6 条已确认：E-217 的伤情分档不按班别，按上表的事实分档。

## 三、导演侧的改动

| 位置 | 改动 |
|---|---|
| `director.ts` `contextFor` | 新增 qualifier `伴侣在册`（读 `伴侣-在册`）、`已确定施暴者`（读 `伤医-施暴者已确定`）、`再次归零`（`recordedSanBreaks(r)>=1` 或 `r.exhausted>=1`）、`家人关系 ≤1` |
| `director.ts` `forceZeroEvent` | 再次归零时按上表返回 E-230／E-233／E-238，首次归零保持原分支 |
| `director.ts` `afterAuthoredChoice` | E-217／E-232／E-239 调用链末结算；E-218-c 写 `early-discharge:<uid>` 并出院；E-219-c／E-220-b／E-220-c 写 `tampered:<uid>` 与 `tampering-discovered:<uid>`；E-224-a 按 `benefitsReceived − benefitsReturned` 退款；E-234-b 第二次触发写 `危机-失联`；E-055 与 E-143 同时成立写 `互相把柄` |
| `director.ts` `buildAuthoredEvents` | 医闹链之外的入口：D6 起，本局有 `damage>=1` 且投诉未化解的患者时写 `伤医-施暴者已确定` 与 `伤医-家属:<uid>`，并记 actorId |
| `director.ts` `chooseBinding` | `DARK_CHAIN_GATES` 承载触发串写不出的前提（E-216 的接触概率随退出安排下降、E-221 需要实际损害、E-225 需要回扣或统方、E-236 需要再次归零、E-241 需要失约记录、E-256 需要在床患者、E-258 需要被压下的问题）。伤医链只绑定纠纷升级的那位患者，出院链只绑定实际提前出院的患者 |
| `followups.ts` | 医闹三拍 escalated 出口后，患者 `damage>=1` 时写 `伤医-施暴者已确定`、`伤医-家属:<uid>`、`伤医-施暴者:family:<uid>` |
| `family-milestones.ts` | 婚礼日卡与丧亲变体后分别由 E-249、E-252 承接，既有写入不变 |
| `catalog.ts` `contextualOptions` | E-216、E-229、E-230、E-241、E-251、E-252 的选项按本局实际产生的人、复印件与陪同显示；E-216 的检定 DC 随退出安排从 14 降到 11 或 8 |
| `fact-records.ts` | `RECORD_ONLY_EVENT_FACTS` 增加 44 条已完成行为条目，含 `举报人`、`知情`、`互相把柄`、`药代-约谈-不实`、`药代-串供` |

补齐的三处旧缺口：E-158 的举报分支写 `举报人`；E-054 的沉默分支写 `知情`；E-055 与 E-143 同时成立写 `互相把柄`。E-140 增加按统一口径答复的选项，写 `药代-约谈-不实` 与 `药代-串供`。

## 四、夜班供给

测量脚本 `npx tsx scripts/scan-events.ts 12`。夜班日为 D3、D6、D9、D12、D14（`RULES.nightDays`）。「可抽」为该夜开始时 `eligibleAuthoredEvents(r,'夜班')` 中权重大于零的条数，同一局内已发过的一局一次条目不再计入。

| 夜班日 | 本分支之前 | E-259–E-264 之后 | E-265–E-274 之后 |
|---|---|---|---|
| D3 | 5–8 | 5–8 | 5–8 |
| D6 | 3–7 | 5–9 | 5–9 |
| D9 | 2–4 | 4–9 | 4–9 |
| D12 | 1 | 4–7 | 8–11 |
| D14 | 1 | 2–5 | 11–15 |

E-259–E-264 的时间窗从 D6、D9、D11 开始，在 D6 与 D9 两夜就被发出。D12 与 D14 的供给因此另加十条：E-265–E-268 从 D10 开始，E-269–E-274 从 D13 开始。D9 与 D12 之间、D12 与 D14 之间各没有夜班日，这两组在对应的夜班日当夜都还没有被发过，两个夜班日的下限不依赖可重复条目（E-165 是全局唯一的可重复夜班条目）。

## 五、验收

| 命令 | 结果 |
|---|---|
| `npm run typecheck` | 通过 |
| `npm run lint` | 通过 |
| `npm test` | 通过，132 个文件 2503 项 |
| `npm run check:content` | 通过，`events: 274`，`errors: []` |
| `npm run check:routes` | 见下节 |
| `TYCHE_ALLOW_MISSING_VOICE=1 npm run build` | 见下节 |

`src/content/events/dark-chains.test.ts` 覆盖：编号连续与阶段解析、124 项事实各有写入方、选项代价的编译值、检定两侧写入不同记录、缺前提不触发、再次归零的互斥组、按本局事实显示的选项、链末结算的取值、身后事项不含方法与准备、已完成行为进入附记、D12 与 D14 的夜班池条数、绑定患者时不虚构床号。`src/game/director.test.ts` 覆盖：再次归零的派发、qualifier 的发布条件、医闹入口的命名条件、链末结算的写入、提前出院与重打页面的记录、退款按实际未退金额计算。

## 六、合并 main 时改到的既有测试

第二次归零以前立即收口，现在先开链。三个文件里断言旧行为的用例按新行为重写，断言的意图保持不变：

| 文件 | 用例 | 改动 |
|---|---|---|
| `src/game/engine-rules.test.ts` | 第二次体力归零 | 由「当日身体结局」改为「开出 E-238，本局继续，`exhausted` 为 2」 |
| | 一次骰子自救之后的第二次 SAN 归零（含读档、含 T23） | 由「收口到 END-33 ＋ X21」改为「开出 E-230，写入 `dark-chain:san`，`sanBreaks` 为 2」 |
| `src/game/interruption-regression.test.ts` | 结算／夜班的重复体力归零 | 由「X17／X20 附件」改为「开链并保留发生阶段」 |
| | 自救不跨阶段续期、旧存档缺计数、T23 只保护一次 | 同上，改为断言开链与计数，不再断言结局 |
| `src/game/storage.test.ts` | 三个策略的完整模拟存档 | 超时预算由 60 秒提到 180 秒：开链的一局要走到 D14，不再在第一周收口 |

## 七、未完成项

- 结局侧（END-01～END-40 的判定、`ending-adapter.ts`、`ending-prose.ts`、结局 CG 与致玩家卡）属工程师 A，本分支未改。
- DK-10（E-244～E-247）只在玩家于轮转登记页选了伴侣的一局里出现。`scripts/audit-routes.ts` 按 `index%3` 轮流使用三种设置，让这四条事件进入路线审计。
- 玩家可见文案改动后的配音重采集（`docs/audio.md` 流程与 `npm run audio:check`）未做。
- 合同第一节 1.4 记「飞检 F 权重 ×1.5 仍未实现」。复核 `director.ts:633` 后确认该权重已经存在（`category===6` 且 `低标入院` 成立时乘 1.5），合同该行已过期，本分支未改。
