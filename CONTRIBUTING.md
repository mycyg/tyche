# 贡献指南

请先阅读 [AGENTS.md](AGENTS.md)、[架构](docs/architecture.md)和[数值规则](docs/design/01_核心机制与数值.md)。全局数值先改设计源，再同步运行镜像；具体病组预算和剧情费用保留各自来源。

问题报告请附浏览器、视口、轮转码、天赋、当天阶段、患者或事件名称、操作顺序与实际结果。存档可能包含玩家自行输入的姓名，请先匿名化；不要提交现实患者资料、凭证或本地音频模型路径。

内容修改需同时核对角色身份、真实前提、患者／项目作用域、费用预览与实际扣款、延迟后果、读档恢复和结局附记。授权、请求、兑现与材料签收分别记录。保留原件与历史事实；未完成的临床步骤不能靠反馈文字变成完成。

## 验证顺序

```sh
npm ci
npm run lint
npm run typecheck
npm test
npm run check:content
npm run simulate -- 100
npm run build
npx playwright install chromium webkit
npm run test:e2e
```

完成一批关联代码修改后集中运行回归。真实入口的路径记录和注入状态的边界测试分别登记；缺少前提的强置状态不计节点覆盖。程序化检查通过后，试玩关键路线，记录整轮问题，再按共同原因修复。

玩家可见文字有改动时，按[配音流程](docs/audio.md)重采集文本、生成对应语音、做质量检查与抽听。`npm run audio:check` 必须通过后才能发布。不要禁用完整性检查，也不要提交已被索引替换的历史音轨、试音或生成模型。

PR 说明写明修改范围、证据、未验证项及存档兼容性。只提交 Tyche 仓库的相关文件，保留其他人的工作。发布构建不得包含密钥、真实患者资料、个人存档、模型权重或开发机器路径。
